# Session timeout in seconds
server.session.timeout=1200

# Cookie max-age in seconds
server.session.cookie.max-age=1200

# Set true when using HTTPS, false for HTTP
server.session.cookie.secure=false